/data/pics failed to load - HMI crashed - why? Try only one .png, allow verbose level 3
Allow user to change update period - min 10ms, max 1000ms during visualization
Allow user to set manual scaLE: MODE AUTO AND MANUAL. When manual is on, button Manual Scale must open window with X-axis and Y-axis scaling as bars, where user can set max and min values
Why device "connect" fail on first time all the time?
When connected session is externally broke (restert server board, cable disconnect...), running visualization must detect this, and show "DISCONNECT" to the centre of the screen and stop pooling. 